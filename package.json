{
  "name": "async-kanren",
  "version": "0.1.0",
  "description": "",
  "main": "build/src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "runindex": "yarn run build && node build/src/index",
    "main": "yarn run build && node build/src/main",
    "cli": "yarn run build && node build/src/cli",
    "bundle": "yarn run build && browserify build/src/web.js -o bundle.js",
    "start": "node build/src/cli"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/atennapel/async-kanren.git"
  },
  "author": "Albert ten Napel",
  "email": "aptennap@gmail.com",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/atennapel/async-kanren/issues"
  },
  "homepage": "https://github.com/atennapel/async-kanren#readme",
  "devDependencies": {
    "@types/node": "^14.6.0",
    "@types/sqlite3": "^3.1.6",
    "@types/uuid": "^8.3.0",
    "browserify": "^16.5.2",
    "typescript": "^4.0.2"
  },
  "dependencies": {
    "sqlite3": "^5.0.0",
    "uuid": "^8.3.0"
  }
}
